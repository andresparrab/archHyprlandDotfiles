monitor=,preferred,auto,1

exec-once=$HOME/.config/hypr/autostart

input {
    kb_layout=se
    kb_variant=
    kb_model=
    kb_options=
    kb_rules=

    follow_mouse=1

    touchpad {
        natural_scroll=no
    }
}

misc{
    
}

general {
    sensitivity=1.0 # for mouse cursor
    main_mod=SUPER
    
    gaps_in=3
    gaps_out=5
    border_size=5
    # col.active_border=0xfff5c2e7
    # col.inactive_border=0xff45475a
    col.active_border=0xff4deeea
    col.inactive_border=0x4deeea
    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)
}

decoration {
    blur_new_optimizations = true
    drop_shadow = true
    shadow_range=100
    shadow_render_power=5
    col.shadow= 0x33000000
    col.shadow_inactive=0x22000000
    rounding=10

    active_opacity=0.9
    inactive_opacity=0.7
    fullscreen_opacity=0.9
    
    blur=1
    blur_size=4 # minimum 1
    blur_ignore_opacity=0
    
    blur_passes=2 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}



animations {
    enabled=1
    # bezier=overshot,0.05,0.9,0.1,1.1
    bezier=overshot,0.13,0.99,0.29,1.1
    animation=windows,1,4,overshot,slide
    animation=border,1,10,default
    animation=fade,1,10,default
   # animation=workspaces,1,6,overshot,slidevert
    animation=workspaces,1,6,default
}

dwindle {
    pseudotile=1 # enable pseudotiling on dwindle
    force_split=0
    col.group_border=0xff89dceb
    col.group_border_active=0xfff9e2af
}

master{
    
}

gestures {
    workspace_swipe=yes
    workspace_swipe_fingers=4
}

exec swayidle -w timeout 5 'systemctl suspend'
$suspend_and_lock=swaylock -f -e -l -L -s fill -i $HOME/.config/hypr/wallpapers/cloud.png && sleep 2 && systemctl suspend

# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
windowrule=move center,title:^(fly_is_kitty)$
windowrule=size 800 500,title:^(fly_is_kitty)$
windowrule=animation slide,title:^(all_is_kitty)$
windowrule=float,title:^(all_is_kitty)$
#windowrule=tile,xy
windowrule=tile,title:^(kitty)$
windowrule=float,title:^(fly_is_kitty)$
windowrule=float,title:^(clock_is_kitty)$
windowrule=size 418 234,title:^(clock_is_kitty)$
#windowrule=pseudo,abc
#windowrule=monitor 0,xyz

# example binds
bind=SUPER,Q,killactive
bind=SUPER,F,fullscreen,1
bind=SUPERSHIFT,F,fullscreen,0
bind=SUPER,RETURN,exec,kitty
bind=SUPER,C,killactive,
bind=SUPERSHIFT,Q,exit,
bind=SUPER,E,exec,thunar
bind=SUPER,W,exec,librewolf
bind=SUPER,b,exec,google-chrome-stable
bind=SUPERSHIFT,V,togglefloating,
bind=SUPER,space,exec,rofi -show drun
bind=SUPER,d,exec,rofi -show drun
# bindsym $mod+space exec wofi --show drun --insensitive --allow-images --prompt=""
# bind=SUPER,P,pseudo,
# bind=SUPER,R,exec,/home/chris/Repos/reStream/reStream.sh
bind=SUPER,ESCAPE,exec,sudo systemctl suspend
bind=SUPER,Y,exec,grim -g "$(slurp)" - | swappy -f -
bind=,XF86AudioMute,exec,pactl set-sink-mute @DEFAULT_SINK@ toggle
bind=,XF86AudioLowerVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ -25%
bind=,XF86AudioRaiseVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ +25%
bind=,XF86AudioMicMute,exec,pactl set-source-mute @DEFAULT_SOURCE@ toggle
bind=,XF86MonBrightnessUp,exec,brightnessctl -q set +25% # increase screen brightness
bind=,XF86MonBrightnessDown,exec,brightnessctl -q set 25%- # decrease screen brightnes
bind=SUPERSHIFT,R,exec,wf-recorder -g "$(slurp)"
bind=SUPERSHIFT,E,exec,wlogout
# bind=SUPERSHIFT,L,exec,swaylock

bind=SUPERSHIFT,RETURN,layoutmsg,swapwithmaster
bind=SUPER,j,layoutmsg,cyclenext
bind=SUPER,k,layoutmsg,cycleprev

bind=SUPER,h,movefocus,l
bind=SUPER,l,movefocus,r
# bind=SUPER,k,movefocus,u
# bind=SUPER,j,movefocus,d

# bind=SUPER,k,cyclenext
# bind=SUPER,j,cyclenext,prev
bind=SUPER,left,resizeactive,-40 0
bind=SUPER,right,resizeactive,40 0

bind=SUPERSHIFT,h,movewindow,l
bind=SUPERSHIFT,l,movewindow,r
bind=SUPERSHIFT,k,movewindow,u
bind=SUPERSHIFT,j,movewindow,d

bind=SUPER,s,movetoworkspace,special
bind=SUPER,n,togglespecialworkspace,

wsbind=1,DP-2
wsbind=2,DP-2
wsbind=3,DP-2
wsbind=4,DP-2
wsbind=5,DP-2
wsbind=6,DP-2
wsbind=7,DP-2

wsbind=8,DP-3
wsbind=9,DP-3
# wsbind=0,DP-3

workspace=DP-2,1
workspace=DP-2,2
workspace=DP-2,3
workspace=DP-2,4
workspace=DP-2,5
workspace=DP-2,6
workspace=DP-2,7

workspace=DP-3,8
workspace=DP-3,9
workspace=DP-3,0

bind=SUPER,1,focusmonitor,DP-2
bind=SUPER,1,workspace,1
bind=SUPER,2,focusmonitor,DP-2
bind=SUPER,2,workspace,2
bind=SUPER,3,focusmonitor,DP-2
bind=SUPER,3,workspace,3
bind=SUPER,4,focusmonitor,DP-2
bind=SUPER,4,workspace,4
bind=SUPER,5,focusmonitor,DP-2
bind=SUPER,5,workspace,5
bind=SUPER,6,focusmonitor,DP-2
bind=SUPER,6,workspace,6
bind=SUPER,7,focusmonitor,DP-2
bind=SUPER,7,workspace,7
bind=SUPER,8,focusmonitor,DP-3
bind=SUPER,8,workspace,8
bind=SUPER,9,focusmonitor,DP-3
bind=SUPER,9,workspace,9
bind=SUPER,0,focusmonitor,DP-3
bind=SUPER,0,workspace,0

bind=SUPERSHIFT,1,movetoworkspacesilent,1
bind=SUPERSHIFT,2,movetoworkspacesilent,2
bind=SUPERSHIFT,3,movetoworkspacesilent,3
bind=SUPERSHIFT,4,movetoworkspacesilent,4
bind=SUPERSHIFT,5,movetoworkspacesilent,5
bind=SUPERSHIFT,6,movetoworkspacesilent,6
bind=SUPERSHIFT,7,movetoworkspacesilent,7
bind=SUPERSHIFT,8,movetoworkspacesilent,8
bind=SUPERSHIFT,9,movetoworkspacesilent,9
bind=SUPERSHIFT,0,movetoworkspacesilent,10


# bind=SUPER,left,resizeactive,-20 0
# bind=SUPER,right,resizeactive,20 0
$suspend_and_lock=swaylock -f -e -l -L -s  && sleep 1 && systemctl suspend
